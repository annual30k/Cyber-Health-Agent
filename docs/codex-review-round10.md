# 第十轮：渐进建议与确认必须共享状态机

82项现有测试独立复跑通过。保持Gemini3.8，保留Codex测试，禁止真实数据/配置写入。第九轮新增逻辑仍有以下代码级缺陷，请优先修复，不扩展无关功能：

1. _evaluate_exercise_progression 遇completion_rate<1直接continue，会跳过最近失败训练，回看更早两次成功并错误提议加重。必须先选同动作最近两次有效记录，再判两次是否均完成/达标；未完成应打断连续达标，而不是从证据中消失。增加成功→成功→最近失败的回归、同一天拆分记录去重/会话标识测试。
2. confirm_training_progression 只校验restricted，完全不检查deload、近期疲劳、多项约束；source_record_ids可为空或任意字符串，confirmed_weight_kg任意正值便入库。该接口名为确认建议，但既不要求待确认建议ID，也不验证来源归属/动作/有效状态/建议负荷。建立可追溯持久建议或等效签名摘要，确认时事务内重算安全与证据版本，变更/过期/跨用户/未知来源拒绝，幂等重放原结果。与手工记录当前负荷分开，避免伪造“由达标建议确认”的证据链。
3. 建议计算还需检查两次记录同负荷/可比较目标与组数，不能只看任意一组达标；缺组数/完成证据/RPE不得默认成功。所有阈值和缺失信息需如实披露。支持加次时确认接口也需匹配，不要只给重量字段。

请添加真实Core及stdio调用边界测试，报告通过数与剩余缺口。不能将通过82测试说成安全状态机已完整。修复后交接Codex复验。
