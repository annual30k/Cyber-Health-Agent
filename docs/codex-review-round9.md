# 第九轮：补齐训练核心闭环

2026-09-05，用户再次要求继续。73项测试已由Codex独立复跑通过，第八轮营养两项已修复。继续Gemini3.8开发，Codex审核；保留独立断言，禁止真实用户数据/配置/Vault写入。

## 先纠正文档不实

traceability-gap-matrix G-3宣称已实现连续2次达到次数上限且RPE<=8的Double Progression，但_evaluate_training_prescription未查询历史workout，当前service中的RPE引用仅录入。原规格4.2明确这是核心状态机，不是可移到P1的复杂周期化。不要把实际未实现功能写成已实现。矩阵遗漏的代码缺口必须真实披露。

## 本轮实现范围

1. 实现规格4.2的训练表现状态机：按用户和稳定动作标识匹配同一动作最近两次有效已完成记录，确定目标次数上限/实际组次/负荷/RPE/恢复状态；不足证据不加重，达标仅生成待用户确认的加重或加次建议，不直接修改已承诺计划。红旗/减载/疲劳优先。记录建议依据的source IDs与确认修订链。补足结构化输入与MCP契约、端到端测试，非目标动作/未完成/缺RPE/一条记录/跨用户均不可触发。
2. 修复训练多约束组合漏洞：恢复分支哑铃+膝限制选Romanian Deadlift但不再检查腰限制；杠铃+腰限制选Squat却忽略膝限制；当前策略为if替换而非对所有约束最终校验。使用动作属性与统一约束过滤，覆盖所有安全分支；所有候选不满足限制时返回信息不足/暂停具体动作与寻求专业指导，不生成相互矛盾处方。不要自行声称医疗处方安全已临床验证。
3. 近期证据当前day<=date无限回溯，把数月前的睡眠用于今天。定义可配置且明确披露的有效窗口，缺失/过期数据按未知处理，不伪造健康或恢复分；补时钟可控测试。
4. 对照4.2补结构化休息时长、已知负荷/次数目标以及同动模式替换，不把未知负荷捏造为确定数值。能在本轮安全完成的实现，剩余明确列真实代码缺口。

请完成后交接实际测试证据和剩余清单，保持默认6工具不变、扩展工具依实际能力注册。
