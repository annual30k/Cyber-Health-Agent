# 第十二轮：完成动态日程核心契约

104项测试已独立通过，真实metrics睡眠不足确认加重回归已通过。下一阶段保持Gemini3.8，保留Codex测试，禁止正式OpenClaw配置/真实提醒/真实Vault和健康库操作。

现有日程只有4个固定窗口，get_schedule仅返回pending/overdue。原规格10.1明确宿主必须获知修改/取消才能撤销定时器，但取消/跳过事件被过滤，宿主无从同步。10.2的晚餐19:30、当餐未记且允许提醒、当日有训练未完成、晨间未commit等条件也尚未体现在接口中。矩阵不能把固定时间表宣称动态闭环。

本轮范围：
1. 实现稳定事件标识与revision完整同步契约：提供取消/跳过等墓碑或可明确解释的全量快照，不让宿主遗留旧定时器。单餐区分午晚事件，计划推迟同步不能被再次生成默认窗口覆盖。
2. 明确返回trigger_condition/当前eligible及抑制原因，结合本地档案提醒偏好、餐食、计划commit、训练完成事实。保持唤醒时重查当前事实。默认6工具不变。
3. 所有状态变化使用一致版本与审计策略；get_schedule当前读取会直接修改overdue但不更新state_version/operation_log，需改为纯派生读取或带审计维护写入口，ToolAnnotations与真实行为一致。
4. 真正stdio测试：拉取→延期→重新拉取→取消→宿主模拟同步；已经记餐/关闭提醒/训练完成/已commit抑制；跨时区和重放稳定。仅模拟宿主，不创建真实定时器。

另一个未完成需求为6.1随路/惰性维护：当前service只有maintain_memory定义，没有daily_review/get_today触发。请先在文档真实标未实现；遵循openclaw-adapter-spec的P0只读纯度，后续通过宿主维护提示/显式维护调用达成，不擅自在只读get_today里写库或启动无界后台线程。本轮优先收口日程，完成后交接。
