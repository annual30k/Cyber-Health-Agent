# 第六轮审核与下一阶段实施要求

用户要求继续由 Gemini 3.8 开发、Codex 审核。52 项既有测试独立通过，新增 tests/test_codex_import_validation.py 四项全部失败：未知 0.999.999 schema、损坏 goals_json、不存在时区、非法 safety_mode 均成功导入。损坏 JSON 还能令后续 get_profile 失败。

请保持 Gemini 3.8，保留全部 Codex 测试与断言。不要只针对四项增加 if：建立统一备份结构验证层，精确版本支持表，严格 JSON 解析及嵌套类型、有效日期时区、枚举、有限非负数与范围、用户归属、引用链和所有持久字段校验；校验和不能替代这些检查。幂等冲突优先于新请求版本验证。既有用户禁止过时备份回退安全状态，包括 deload 截止时间。对所有冲突整笔回滚。全字段比对仍遗漏 meal causation_id/state_version 与审计载荷，不可声称全量比对。

本轮可以在完成导入契约修复后继续以下明确范围，不必停在文档交付：

1. 对照原需求和 openclaw-adapter-spec，接通已有 Core 能力到扩展 MCP 层，保持默认六工具白名单不变。完善严格输入模型和真实 stdio 调用测试，不仅测试工具数量。新增工具数量应由真实接口清单决定。
2. 对已接通接口设置真实安全 annotations：只读、破坏性、幂等与外部世界属性不可一刀切。幂等仅代表相同键重试安全，不能降低健康数据写入审批。
3. 为完整需求制作逐条可追溯的缺口清单，区分未写代码与缺外部 Provider。双层查询和 TTL 本地趋势聚合不能全部用外部依赖掩盖。没有真实 Provider 不得宣称 Vault 集成完成。

不要更改正式 OpenClaw 配置、用户 Vault、真实健康数据、安装插件或建立提醒。只能临时库/模拟 Provider/隔离宿主验证。Codex仅写独立验收文件，不并行改业务实现。完成后报告真实测试结果与剩余缺口，不能声称全需求已完成。
